import React from 'react';

const Fim = () => {
  const screenStyle = {
    width: '100vw',
    height: '100vh',
    backgroundColor: 'black',
  };

  return <div style={screenStyle}></div>;
};

export default Fim;
